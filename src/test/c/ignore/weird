{
  // This parses as an indexed variable access (good)
  foo[2]

  // Syntax error
  foo[1, 2, 3]

  // Syntax error
  foo[1,]

  // This parses as a variable followed by a list literal
  foo
  [2]
}
