// Test variable access modes

// Plain identifiers
foo
bar == 5
xyz < bar + 1

// Struct members
book.isbn
listing.creator.display_name
book.title == "Book Title"

// Indexed variables
foo[5]
foo[bar.baz]
foo.bar["hello" + ", world!"].baz
